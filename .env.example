COMPOSE_PROJECT_NAME=wordpress
# Docker Official image, Apache/mod_php. WordPress 7.1.0 (latest 7.1 stable)
# is not yet published as a Docker Hardened Image (DHI's newest WordPress
# build tops out at 7.0.4), so this stays on the Docker Official image until
# DHI catches up. docker/wordpress/Dockerfile supports both base-image
# families (DHI `-fpm-*-dev` and Docker Official `-apache`) and auto-detects
# which one it is building on.
WORDPRESS_BASE_IMAGE=wordpress:7.1.0-php8.3-apache
MARIADB_IMAGE=dhi.io/mariadb:11.8-debian13-dev
WORDPRESS_PORT=8888
PHPMYADMIN_PORT=8889
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=wordpress
MYSQL_ROOT_PASSWORD=root
WORDPRESS_URL=http://localhost:8888
WORDPRESS_TITLE=Flavor Agent Local
WORDPRESS_ADMIN_USER=admin
WORDPRESS_ADMIN_PASSWORD=admin
WORDPRESS_ADMIN_EMAIL=admin@example.com
# Optional: absolute path to a local WordPress root for `npm run lint:plugin`
WP_PLUGIN_CHECK_PATH=
# Set to 1 on Windows/Docker Desktop to run Plugin Check inside the local
# WordPress container instead of requiring host access to Docker volume paths.
PLUGIN_CHECK_USE_DOCKER=
WORDPRESS_DB_HOST=
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=wordpress
FLAVOR_AGENT_WP70_BASE_IMAGE=wordpress:7.1.0-php8.2-apache
FLAVOR_AGENT_WP70_PORT=9404
FLAVOR_AGENT_WP70_PHPMYADMIN_PORT=9405
