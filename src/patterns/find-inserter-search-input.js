/**
 * Inserter search input discovery.
 *
 * This module re-exports from the centralized compatibility adapter
 * (./compat.js). Import from './compat' for new code; this file
 * exists for backward compatibility with existing test imports.
 */
export { findInserterSearchInput } from './compat';
