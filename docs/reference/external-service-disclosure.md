# External Service Disclosure

Date: 2026-05-19

This is the canonical WordPress.org disclosure inventory for Flavor Agent. Keep the `readme.txt` external-services block 1:1 with this document whenever outbound call sites, provider routing, background jobs, or data sent to providers changes.

## Submission Summary

Flavor Agent does not send data to external services on plugin activation. Outbound calls are made only after one of these events:

- An administrator configures the related backend in `Settings > Connectors` or `Settings > Flavor Agent`.
- An authorized editor/admin explicitly requests a recommendation, validation, pattern sync, or docs search.
- A background job runs because it was scheduled by one of those configured or user-triggered actions.
- A developer explicitly enables the optional core-roadmap or public-docs-prewarm filters.

Browser JavaScript calls only the local WordPress REST API for this site. Third-party calls are server-side through the WordPress HTTP API or delegated to the WordPress AI Client / Connectors runtime.

## Service Inventory

| Service                                                     | Code paths                                                                                                                                                                                                         | Endpoint ownership                                                                                                                                                          | Data sent                                                                                                                                                                                                                                                                                                                            | Trigger                                                                                                                                                                                                                                                                                                  | Setup / gating                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| OpenAI chat through `Settings > Connectors`                 | `inc/LLM/ChatClient.php`, `inc/AzureOpenAI/ResponsesClient.php`, `inc/LLM/WordPressAIClient.php`; recommendation ability classes under `inc/Abilities/`                                                            | Connector-owned by WordPress AI Client. Flavor Agent first uses `WordPress\AI\get_ai_service()->create_textgen_prompt()` when available, then falls back to `wp_ai_client_prompt()`, without using saved provider values from older settings screens.                         | System and user prompts for the requested recommendation surface. Depending on surface, prompts can include selected block context, current post/page content, templates/template parts, navigation markup, Global Styles or Style Book context, guidelines, and cached docs snippets.                                               | Authorized user requests a recommendation through an editor panel or Abilities API.                                                                                                                                                                                                                     | Requires a configured text-generation provider in `Settings > Connectors`. No cron chat calls.                                                                                                                                                                                                                                                                                 |
| Anthropic, Google, and other connector-backed chat providers | Same chat path as OpenAI connector-backed chat. Connector ID examples are discovered by `inc/OpenAI/Provider.php`.                                                                                                 | Connector-owned by WordPress AI Client and the installed provider connector plugin.                                                                                         | Same recommendation prompt data as connector-backed chat above.                                                                                                                                                                                                                                                                      | Authorized user requests a recommendation and the WordPress AI Client routes text generation to that connector.                                                                                                                                                                                          | Requires the relevant connector to be installed, available for text generation, and configured in `Settings > Connectors`.                                                                                                                                                                                                                                                       |
| Cloudflare Workers AI embeddings                            | `inc/Cloudflare/WorkersAIEmbeddingConfiguration.php`, `inc/Embeddings/EmbeddingClient.php`, `inc/OpenAI/Provider.php`, `inc/Admin/Settings/Validation.php`, `inc/Patterns/PatternIndex.php`, `inc/Abilities/PatternAbilities.php` | Site-owner Cloudflare account endpoint `https://api.cloudflare.com/client/v4/accounts/{account}/ai/v1/embeddings`.                                                         | Validation probe text (`validation`), pattern index signature probe text when the saved signature cannot be reused, registered/synced pattern text, pattern metadata included in embedding text, and Qdrant pattern-search query text when Qdrant pattern storage is used.                                                                                                     | Settings save validation, Qdrant-backed manual/admin pattern sync, Qdrant-backed scheduled pattern sync, and Qdrant-backed pattern recommendation retrieval. The Cloudflare AI Search pattern backend bypasses plugin-owned embeddings for sync and retrieval.                                                                                              | Requires saving account ID and API token in `Settings > Flavor Agent`; a blank model field uses the default Workers AI embedding model `@cf/qwen/qwen3-embedding-0.6b`. Qdrant-backed pattern recommendations also require Qdrant and chat through `Settings > Connectors`.                               |
| Qdrant                                                      | `inc/Embeddings/QdrantClient.php`, `inc/Patterns/PatternIndex.php`, `inc/Abilities/PatternAbilities.php`, `inc/Admin/Settings/Validation.php`                                                                     | Site-owner Qdrant cluster URL saved in `Settings > Flavor Agent`.                                                                                                           | Validation and health requests; collection definitions; vectors; point payloads containing pattern name/title/description, categories, block/template metadata, inferred traits, source/type, synced-pattern identifiers/status, and pattern content; query vectors and filters for pattern retrieval.                               | Settings save validation, manual/admin pattern sync, scheduled pattern sync, and pattern recommendation retrieval when the Qdrant pattern backend is selected.                                                                                                                                             | Requires Qdrant URL and API key in `Settings > Flavor Agent`. Qdrant sync is only queued when both embeddings and Qdrant are configured and the Qdrant backend is selected.                                                                                                                                                                                                      |
| Cloudflare AI Search for private pattern retrieval          | `inc/Cloudflare/PatternSearchClient.php`, `inc/Patterns/PatternIndex.php`, `inc/Patterns/Retrieval/CloudflareAISearchPatternRetrievalBackend.php`, `inc/Abilities/PatternAbilities.php`, settings validation       | Site-owner private Cloudflare AI Search endpoint `https://api.cloudflare.com/client/v4/accounts/{account}/ai-search/namespaces/patterns/instances/{instance}`.               | Managed-instance list/create calls; owner-marker reads/uploads; pattern item uploads containing pattern title, description, categories, block/template metadata, inferred traits, public-safe pattern content, source/type, synced identifier when available, and public-safety metadata; item list/delete requests; recommendation query text; `visiblePatternNames` nested as `ai_search_options.retrieval.filters.pattern_name`. | Settings save validation, manual/admin pattern sync, scheduled pattern sync, stale-item reconciliation, and pattern recommendation retrieval when the Cloudflare AI Search pattern backend is selected. During sync, Flavor Agent deletes only stale remote items that were recorded in its previous fingerprint state; unknown remote items and the owner marker are preserved. | Requires selecting the Cloudflare AI Search pattern backend and saving Cloudflare Workers AI Embedding Model account ID and API token in `Settings > Flavor Agent`; blank or unsupported model values use `@cf/qwen/qwen3-embedding-0.6b` for the private AI Search pattern index. Existing deterministic `flavor-agent-patterns-{site_hash}` instances are adopted only after validating metadata schema, owner marker, and normalized AI Search embedding model. Newly created instances are created with the expected schema/model, then owner-marker validated before the signature is stored for sync/retrieval use. This private pattern backend is separate from the built-in public WordPress developer-docs AI Search endpoint. Pattern reranking still requires chat through `Settings > Connectors`. |
| Cloudflare AI Search for WordPress developer docs grounding | `inc/Cloudflare/AISearchClient.php`, `inc/Support/CollectsDocsGuidance.php`, recommendation ability classes that collect docs guidance, `inc/Abilities/WordPressDocsAbilities.php`            | Built-in public Cloudflare AI Search endpoint for trusted WordPress developer sources. The accepted corpus is stable `developer.wordpress.org` handbook/reference content plus current Developer Blog and Make/Core release-cycle posts. | Docs search queries derived from recommendation context, such as block names, template/template-part/navigation/style context, and the user's recommendation prompt, plus arbitrary administrator-supplied docs search queries submitted through the `search-wordpress-docs` ability. The service returns trusted snippets with source and freshness metadata used as prompt grounding. | Cache lookups are local only. Live searches can happen during an authorized user recommendation when no specific cache exists, through an async context-warm job queued by that recommendation, or through the manage-options docs search ability. Editor/functionality recommendation surfaces fail closed before chat when no trusted docs grounding is available; trusted stale/degraded guidance can proceed with warnings. The editorial `recommend-content` lane is exempt. | The built-in public endpoint is available for user-triggered docs grounding and requires no site-owner Cloudflare credentials. It is not prewarmed on activation. Background prewarm requires the explicit `flavor_agent_cloudflare_ai_search_allow_public_prewarm` opt-in filter. Public corpus ownership and refresh requirements live in `docs/reference/developer-docs-public-corpus-runbook.md`. |
| GitHub core roadmap guidance                                | `inc/Support/CoreRoadmapGuidance.php`, `inc/Support/CollectsDocsGuidance.php`                                                                                                                                      | Public GitHub project page `https://github.com/orgs/WordPress/projects/240/views/7?layout=table&hierarchy=true`.                                                            | A GET request for the public roadmap page. GitHub receives normal HTTP metadata such as server IP address and user agent; Flavor Agent does not send post content or editor prompts in the request body.                                                                                                                             | Optional cache warm when roadmap guidance is enabled and cache is cold. Can schedule `flavor_agent_warm_core_roadmap_guidance`.                                                                                                                                                                          | Off by default. Requires the `flavor_agent_enable_core_roadmap_guidance` filter to explicitly opt in. Collection and cron callbacks no-op when disabled.                                                                                                                                                                                                                       |

Recommendation-time Developer Docs grounding has two checks: the per-request guidance must contain trusted official Developer Docs chunks, and the built-in public corpus coverage check reports whether the managed endpoint currently includes a stable Developer Docs source plus a current Make/Core or Developer Blog release-cycle source. Release-cycle coverage is current only when the source post has a recent `published_at`; a recent crawl timestamp does not make an old dated post current. The source-coverage check is cached and may be refreshed by normal recommendation/docs requests. Signature-only freshness checks read local caches only: they do not run live Developer Docs searches, schedule Developer Docs context warms, schedule Core roadmap warms, or persist runtime diagnostics. Missing current release-cycle coverage is diagnostic by default and becomes blocking only when `FLAVOR_AGENT_DOCS_GROUNDING_REQUIRE_CURRENT_COVERAGE` or the `flavor_agent_docs_grounding_require_current_coverage` filter explicitly enables the release gate. When the last confirmed current corpus snapshot is within the seven-day coverage grace window, `missing-current-release-cycle` proceeds with the trusted-but-degraded warning even if the release gate is enabled; `missing-developer-docs` and transport failures remain blocking.

## Cron And Background Job Audit

| Hook                                      | External service                                 | Scheduling path                                                                                                                                              | Can schedule before setup?                                                                                                                                                                                                                         | Fire-time behavior                                                                                                                                                                                                        |
| ----------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `flavor_agent_reindex_patterns`           | Cloudflare Workers AI embeddings plus Qdrant, or private Cloudflare AI Search pattern retrieval | `PatternIndex::activate()`, registry/dependency change handlers, retry handling, and manual sync enqueueing schedule this hook. | No. Scheduling returns unless the selected pattern backend is configured: embeddings plus Qdrant for Qdrant, or saved Cloudflare account/token plus the normalized AI Search embedding model from the Embedding Model section and a validated managed Cloudflare AI Search pattern instance for Cloudflare AI Search.                                    | `PatternIndex::sync()` probes the selected backend before writes. Qdrant sync validates active embeddings/Qdrant and upserts/deletes points. Cloudflare AI Search sync lists items, uploads changed public-safe patterns with `wait_for_completion=false`, preserves unknown remote items and the owner marker, waits for listed current pattern items to report `completed`, and retries pending or retryable failed item processing such as Workers AI capacity errors. |
| `flavor_agent_prewarm_docs`               | Cloudflare AI Search                             | `AISearchClient::schedule_prewarm()` on activation/init.                                    | No. Public endpoint prewarm is blocked by default and requires explicit filter opt-in. | `AISearchClient::prewarm()` re-checks the prewarm gate. Without opt-in it writes an off/not-configured state and makes no remote request.                                                                  |
| `flavor_agent_warm_docs_context`          | Cloudflare AI Search                             | `AISearchClient::schedule_context_warm()` queues exact/family/entity cache warming after a user-triggered recommendation had a cache miss or broad fallback. | No activation scheduling. It is scheduled only as a consequence of an authorized recommendation/docs request.                                                                                                                                      | `process_context_warm_queue()` clears stale schedules, no-ops if docs grounding is unavailable, normalizes queued entries, retries with bounded backoff, and clears invalid or completed entries.                         |
| `flavor_agent_warm_core_roadmap_guidance` | GitHub                                           | `CoreRoadmapGuidance::schedule_warm()` on activation/init or cold-cache collection.                                                                          | No by default. `CoreRoadmapGuidance::is_enabled()` defaults false and requires the `flavor_agent_enable_core_roadmap_guidance` filter.                                                                                                             | `CoreRoadmapGuidance::warm()` re-checks the same opt-in. When disabled it returns without remote requests.                                                                                                                |

## Pattern Public-Safety Notes

Cloudflare AI Search for private pattern retrieval uses two public-safety layers:

- Sync-time filtering uploads only public-safe registered patterns and published user `wp_block` patterns across synced, partial, and unsynced states. Private, draft, trashed, or explicitly non-public-safe user patterns are not uploaded.
- Retrieval-time rehydration treats the remote index as stale/untrusted. If a synced pattern's status or permissions change between sync runs, the recommendation request rehydrates the current `wp_block` post and drops candidates that are no longer published and readable before ranking or response output.

## WordPress.org Readme Disclosure Checklist

The readme must disclose these services by name:

- OpenAI
- Cloudflare Workers AI
- connector-backed chat providers, including Anthropic and Google as examples
- Qdrant
- Cloudflare AI Search for private pattern retrieval
- Cloudflare AI Search for WordPress developer-doc grounding
- GitHub

For each, the readme must state:

- What data is sent.
- When it is sent.
- What setup or explicit action is required first.
- That activation alone does not send data to the service.
- Links to provider terms and privacy policies.

## Provider Policy Links

These links are mirrored in `readme.txt` for reviewer convenience:

- OpenAI: [Terms](https://openai.com/policies/terms-of-use/) · [Privacy](https://openai.com/policies/privacy-policy/)
- Anthropic: [Commercial Terms](https://www.anthropic.com/legal/commercial-terms) · [Privacy](https://www.anthropic.com/privacy)
- Google Gemini API: [Additional Terms](https://ai.google.dev/gemini-api/terms) · [Privacy](https://policies.google.com/privacy)
- Qdrant: [Terms](https://qdrant.tech/legal/terms_and_conditions/) · [Privacy](https://qdrant.tech/legal/privacy-policy/)
- Cloudflare: [Terms](https://www.cloudflare.com/terms/) · [Privacy](https://www.cloudflare.com/privacypolicy/)
- GitHub: [Terms](https://docs.github.com/en/site-policy/github-terms/github-terms-of-service) · [Privacy](https://docs.github.com/en/site-policy/privacy-policies/github-general-privacy-statement)
