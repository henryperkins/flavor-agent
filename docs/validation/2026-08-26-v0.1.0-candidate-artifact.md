# v0.1.0 Candidate Artifact Verification - 2026-08-26

## Candidate identity

- Commit: `e38fb5700d90b7a2a988fa59e2a62cca65879db8`
- Branch at verification time: `master`
- Commit time: `2026-08-26T07:21:31Z`
- Subject: `Record v0.1.0 release evidence`
- Worktree state before strict verification and distribution build: clean
- Final `v0.1.0` tag or release created: **no**

This is clean-commit candidate evidence. It is not exact-tag evidence.

## Strict verification

An initial unqualified `npm run verify -- --strict` attempt is not counted as
release evidence. It recorded seven passing steps, one skipped Plugin Check
prerequisite, and one Docker setup failure before the WordPress 7.1 suite ran.
The causes were local environment values: blank Plugin Check settings caused the
availability probe to resolve `/`, the local image override still named
WordPress 7.0, and the default `dhi.io` MariaDB image required registry
authentication.

The release-evidence rerun used explicit, process-local overrides. No repository
or WordPress credential setting was changed:

```bash
PLUGIN_CHECK_USE_DOCKER=1 \
MARIADB_IMAGE=mariadb:11.4 \
FLAVOR_AGENT_WP70_BASE_IMAGE=wordpress:7.1.0-php8.2-apache \
npm run verify -- \
  --strict \
  --output=output/verify-e38fb57-rerun
```

Result:

```text
VERIFY_RESULT={"status":"pass","summaryPath":"output/verify-e38fb57-rerun/summary.json","counts":{"total":9,"passed":9,"failed":0,"skipped":0}}
```

- Started: `2026-08-26T07:29:31.221Z`
- Finished: `2026-08-26T07:45:14.066Z`
- Duration: `942,845ms`
- Summary SHA-256:
  `484ead2e1c54ddb9a5461e4ed9e249fddd00537401f44a739f5bdfe348e01f89`

| Step | Result | Duration |
| --- | --- | ---: |
| `build` | Pass | `25,278ms` |
| `lint-js` | Pass | `35,019ms` |
| `lint-plugin` | Pass; staged 209-file tree, no errors | `143,281ms` |
| `unit` | Pass; 112 suites / 1,752 tests | `32,529ms` |
| `lint-php` | Pass | `12,555ms` |
| `check-docs` | Pass | `512ms` |
| `test-php` | Pass; 2,244 tests / 10,526 assertions | `5,154ms` |
| `e2e-playground` | Pass; 17/17 on WordPress 7.1 / PHP 8.3 | `297,508ms` |
| `e2e-wp70` | Pass; 30/30 on WordPress 7.1 / PHP 8.2.33 | `390,750ms` |

Environment recorded by the verification runner:

- Linux `x64`
- Node `v24.19.0`
- npm `11.17.0`
- Composer `2.10.2`
- CLI PHP `8.5.4`
- Docker `29.7.2`
- Playground: WordPress `7.1`, PHP `8.3`
- Docker Site Editor: `wordpress:7.1.0-php8.2-apache`, WordPress `7.1`,
  PHP `8.2.33`, AI plugin `1.3.0`, Flavor Agent `0.1.0`

Complete successful-run logs and `summary.json` are bundled locally at:

`output/release-e38fb57/verify-logs-e38fb57.zip`

- Bundle SHA-256:
  `0fadb388156f065113f03e514b66660f46e90b7a62b3afaa6a957d33d90d1686`
- Size: `13,487` bytes
- Contents: the summary plus all 18 stdout/stderr step logs

The log bundle is ignored local output and must be attached to the release or
another durable public record under the approved hosted-CI waiver. The
infrastructure-only approval and its retained gates are recorded in
[`2026-08-26-github-actions-zero-step.md`](2026-08-26-github-actions-zero-step.md).

## Distribution artifact

Command:

```bash
npm run dist
```

Result:

- Artifact: `dist/flavor-agent.zip`
- Built: `2026-08-26T07:48:10Z`
- Size: `1,229,877` bytes
- SHA-256:
  `52241a35a7fa5103eda184a9b550546802d1dc24f727683eeb156d073f269c18`
- Archive root: one `flavor-agent/` directory
- Files: `209`
- Directories after extraction: `29`
- Symlinks: `0`

The build repeated the production Webpack build successfully. Webpack emitted
its existing three advisory size warnings for `index.js`, `activity-log.js`,
and their entrypoints; it emitted no build error.

Inventory checks confirmed these required files are present:

- `flavor-agent/flavor-agent.php`
- `flavor-agent/readme.txt`
- `flavor-agent/LICENSE`
- `flavor-agent/build/index.js`
- `flavor-agent/build/admin.js`
- `flavor-agent/build/activity-log.js`
- `flavor-agent/vendor/autoload.php`

The archive contains none of the following:

- `.git` or `.worktrees`
- `.env` files or an Anthropic key-like value
- `tests`, `node_modules`, or development Composer packages
- `src`, `scripts`, `.github`, or contributor documentation
- `output`, `dist`, local Playwright output, or verification logs
- `composer.lock`, `package.json`, or `package-lock.json`

The archive inventory and extracted-tree count agree at 209 files.

## Plugin Check against the zip payload

The archive was extracted to
`output/release-e38fb57/extracted/flavor-agent`. Plugin Check targeted that
extracted archive tree through its container-visible path, not the repository
root and not a newly prepared staging directory.

Release-gated command:

```bash
docker compose exec -T wordpress wp plugin check \
  /var/www/html/wp-content/plugins/flavor-agent/output/release-e38fb57/extracted/flavor-agent \
  --path=/var/www/html \
  --exclude-directories=vendor,node_modules \
  --ignore-codes=WordPress.NamingConventions.PrefixAllGlobals.NonPrefixedHooknameFound,WordPress.NamingConventions.PrefixAllGlobals.DynamicHooknameFound,WordPressVIPMinimum.Performance.WPQueryParams.PostNotIn_post__not_in,WordPress.DB.SlowDBQuery.slow_db_query_tax_query \
  --slug=flavor-agent \
  --format=strict-table \
  --allow-root
```

Result: exit `0`, `Success: Checks complete. No errors found.`

A second diagnostic pass used the same extracted zip payload and exclusions but
removed all four `--ignore-codes` values and requested JSON format. It also
exited `0` with no findings. Therefore:

- Plugin Check version: `2.1.0`
- Scan WordPress: `7.2-alpha-63347`
- Scan PHP: `8.3.33`
- Errors: `0`
- Warnings: `0`
- Warning dispositions required: none

The four pipeline ignore codes were not needed to make this artifact pass; the
raw diagnostic confirms they hid no current zip-payload finding.

## Dependency and license inventory

- Plugin license: `GPL-2.0-or-later`; the root `LICENSE` ships in the archive.
- PHP runtime packages: none. `composer licenses --no-dev --format=json`
  reports an empty dependency set. The archive contains Composer's generated
  production autoloader and its license, but no development packages or
  `composer.lock`.
- JavaScript direct runtime dependencies: 18 WordPress packages, all declaring
  `GPL-2.0-or-later`:

| Package | Installed version |
| --- | --- |
| `@wordpress/api-fetch` | `7.47.0` |
| `@wordpress/base-styles` | `9.0.0` |
| `@wordpress/block-editor` | `15.20.0` |
| `@wordpress/blocks` | `15.20.0` |
| `@wordpress/components` | `34.0.0` |
| `@wordpress/compose` | `8.0.0` |
| `@wordpress/data` | `10.47.0` |
| `@wordpress/dataviews` | `15.0.0` |
| `@wordpress/date` | `5.47.0` |
| `@wordpress/editor` | `14.47.0` |
| `@wordpress/element` | `7.0.0` |
| `@wordpress/hooks` | `4.47.0` |
| `@wordpress/i18n` | `6.20.0` |
| `@wordpress/icons` | `13.2.0` |
| `@wordpress/notices` | `5.47.0` |
| `@wordpress/plugins` | `7.47.0` |
| `@wordpress/rich-text` | `7.47.0` |
| `@wordpress/theme` | `0.14.0` |

For a conservative view of the production npm lock graph, 599 unique
package/version entries resolve to these declared licenses after normalizing
three legacy `licenses` arrays:

| License | Packages |
| --- | ---: |
| MIT | 464 |
| GPL-2.0-or-later | 67 |
| ISC | 32 |
| BSD-3-Clause | 13 |
| Apache-2.0 | 11 |
| 0BSD | 2 |
| MIT-0 | 2 |
| `(MIT OR Apache-2.0)` | 1 |
| `(MIT OR CC0-1.0)` | 1 |
| Apache-2.0 WITH LLVM-exception | 1 |
| BSD-2-Clause | 1 |
| CC-BY-4.0 | 1 |
| CC0-1.0 | 1 |
| Python-2.0 | 1 |
| Unlicense | 1 |

`computed-style@0.1.4`, `gradient-parser@1.1.1`, and `svg-tags@1.0.0` omit the
modern singular license field in the lock metadata. Each package's checked-in
`LICENSE`/`LICENSE-MIT` file and legacy `licenses` array identify MIT, so no
production-lock entry remains unresolved. The archive excludes `node_modules`;
the lock graph is a conservative source/build inventory around the compiled
assets, not a claim that every package directory ships verbatim.

Dependency manifest identities:

- `package-lock.json` SHA-256:
  `8fd348858ce5dd6e0ccfd92e442bf44fc88f9e9d6407eb616cdcb0ed83b5bf6a`
- `package.json` SHA-256:
  `91b6a4f3fe8801efe76aab1c89f775809a376c2bfe8b20d0e4372daa33373e3f`
- `composer.lock` SHA-256:
  `c1880b7660e6107fff3332df5efd391b97aeff0ce402cd4d18372671f9740207`
- `composer.json` SHA-256:
  `dd6962568a208b684f9d830bcec6ea14a63eb6eb1fa2421b4940589542be70df`

## Release boundary

This closed clean candidate verification and candidate-archive inspection for
commit `e38fb57`. At the time this record was captured, the unavailable
GitHub-hosted runner was covered by the explicit infrastructure-only waiver,
but the verdict remained no-ship until exact-tag verification:

- Hosted GitHub Actions is still non-executable because the account billing
  lock leaves jobs at `runner_id: 0` with zero steps. That missing hosted
  execution is waived for `v0.1.0` only; see
  [`2026-08-26-github-actions-zero-step.md`](2026-08-26-github-actions-zero-step.md).
- At the time this candidate record was captured, `refs/tags/v0.1.0` had not
  been created, the archive had not been rebuilt from a fresh tag checkout,
  and the assets had not been attached. The later GitHub release and its
  `v0.1.0-tag-verification.md` asset are the canonical final evidence.

Tagging and publication authority was provided later in the release session.
The GitHub release and its `v0.1.0-tag-verification.md` asset record execution
of the exact-tag rebuild, independent verification, and attachment steps. This
document remains candidate evidence and did not itself grant that authority.
