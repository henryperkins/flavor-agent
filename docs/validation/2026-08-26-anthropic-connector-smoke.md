# Anthropic Connector Smoke Test - 2026-08-26

Run time: 2026-08-26T06:10:48Z.

Repository identity: `4831b4e000b1f004e00baccdfdfaacffc4bfa861`
(`master`). The working tree was clean before this evidence file was added.

## Result

Status: **pass** for live connector-backed text generation and model
resolution.

Flavor Agent completed one real `recommend-content` request through the
WordPress AI Client and the Anthropic Connector. The request resolved the
explicitly selected `claude-sonnet-4-6` model, returned a schema-valid content
recommendation, and reported no unexpected runtime error. Nothing was applied
or published.

This is local runtime evidence tied to the candidate commit. It is not a
substitute for the final exact-tag verification run. The local stack was on
WordPress trunk (`7.2-alpha-63347`), not the separate WordPress 7.1 browser
harness.

## Environment

- WordPress: `7.2-alpha-63347`
- PHP in the WordPress container: `8.3.33`
- Flavor Agent: `0.1.0`
- AI plugin: `1.3.0`
- Anthropic Connector: `1.0.4`
- Provider: `anthropic`
- Connector plugin slug: `ai-provider-for-anthropic`
- Surface: `flavor-agent/recommend-content`
- Requested and resolved model: `claude-sonnet-4-6`
- Model-selection source: `ai_plugin_feature_developer`
- Model-resolution status: `model`
- Runtime path: `Anthropic via Settings > Connectors`

## Request and response evidence

The request used synthetic release-verification context. It asked for a short,
factual note explaining that recommendations remain reviewable drafts and are
not published automatically. It contained no credentials, private content,
real post content, names, or production-site data.

The returned recommendation was:

- Mode: `draft`
- Title: `Reviewable AI Recommendations`
- Summary: `A clear, factual two-paragraph verification note confirming that AI-generated recommendations are always held as drafts pending human review before any publication.`
- Content:

  > AI recommendations on this site are saved as drafts. They are never
  > published automatically.
  >
  > Every recommendation requires a manual review step before it goes live.
  > That step exists by design. Nothing generated by AI bypasses it.

Runtime diagnostics:

- Latency: `10671 ms`
- Input tokens: `2046`
- Output tokens: `164`
- Total tokens: `2210`
- Unexpected error recorded: `false`
- Recommendation issues returned: `0`

## Credential handling and execution boundary

`ANTHROPIC_API_KEY` was read from the ignored local `.env` file and passed only
as an environment variable to the WordPress container process. The value was
not printed, copied into this file, placed in a command argument, or stored in a
WordPress option. The local `.env` permissions were restricted to mode `0600`
before the request.

The smoke invoked the production `RecommendContentAbility` execution path,
including `RecommendationAbilityExecution`, `ContentAbilities`, `ChatClient`,
the WordPress AI Client, and the Anthropic Connector. The local AI feature
toggle was disabled, so the test instantiated the production ability class and
ran its inherited `WP_Ability::execute()` method rather than changing persistent
feature options merely to expose it through the global ability registry.
