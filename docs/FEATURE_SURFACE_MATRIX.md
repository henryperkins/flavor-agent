# Flavor Agent Feature Surface Matrix

This is the fastest document for answering four questions:

- Where does a Flavor Agent surface appear?
- When does it show up, stay hidden, or degrade?
- What can the user actually do there?
- Does the surface support deterministic apply and Flavor Agent undo?

Use this together with `docs/features/README.md` for per-surface flows and `docs/reference/abilities-and-routes.md` for exact contracts.

For every chat-backed surface, the WordPress AI plugin Connector Approval experiment can gate the request even when the selected connector is configured. Flavor Agent treats that as a request-time setup notice with connector/caller details, not as an unavailable bootstrap capability.

## First-Party Editor And Admin Surfaces

| Surface                                                                    | Exact UI location                                                                                                                                                                                                                                    | Shows when                                                                                                                                                                                                                                                                          | Hidden or limited when                                                                                                                                                                                                                                                                                                                                                                   | What the user can do                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Apply / undo                                                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Block recommendations](features/block-recommendations.md)                 | Native block Inspector `AI Recommendations` panel; fallback document panel keeps the last selected block available after selection clears                                                                                                            | A block is selected or remembered, and that block is not in `disabled` editing mode                                                                                                                                                                                                 | Hidden for disabled blocks; fetch is disabled when `flavorAgentData.canRecommendBlocks` is false but the panel now points admins to `Settings > Connectors` for text generation; content-only blocks stay visible but restrict executable changes to content-safe attributes while still allowing broader advisory guidance                                                              | Follow the shared scope/freshness -> prompt -> explanation -> grouped `Apply now` / `Review first` / `Settings suggestions` / `Style suggestions` / `Manual ideas` lanes -> undo/history model, with stale results using a refresh hero instead of a fresh featured card. The panel keeps explicit inline-apply rationale for safe local block updates and a review-first structural apply lane for validator-approved operations; block, settings, and style apply actions now live only in the main panel, while delegated native Inspector sub-panels mirror the latest result passively for context; selected `core/navigation` blocks also expose embedded navigation guidance | Inline apply for safe local attribute changes; reviewed structural apply for validated selected-block pattern insert/replace operations behind the rollout flag; latest valid inline or structural activity tail can be undone when the live snapshot or structural signature still matches |
| [Pattern recommendations](features/pattern-recommendations.md)             | Native inserter Flavor Agent shelf plus inserter-toggle badge                                                                                                                                                                                        | Post type is known, Connectors chat is configured, and the selected Pattern Storage backend has a ready usable pattern index; passive fetch runs on editor load and active fetch runs from inserter search                                                                            | When Pattern Storage, the Embedding Model, or the usable pattern index is unavailable, opening the inserter now shows a shared setup notice with `Settings > Flavor Agent` guidance for Qdrant Pattern Storage or Cloudflare AI Search Pattern Storage, plus `Settings > Connectors` chat guidance when available; no recommendations when the allowed pattern set is empty; badge stays hidden if no anchor exists or there is no visible state to show | Browse ranked patterns, insert matched allowed patterns from the local shelf, see badge state, or learn which storage or setup path is missing before trying again; this is intentionally a ranking/browse-only surface rather than a lane/review/apply UI, and it no longer rewrites Gutenberg's native pattern registry                                                                                                                                                                                                                                                                                       | No Flavor Agent undo; inserted patterns still enter the document through core block insertion flow                                                                                                                                                                                          |
| [Content recommendations](features/content-recommendations.md)             | Post/page editor `PluginDocumentSettingPanel` named `Content Recommendations`                                                                                                                                                                        | The current edited post type is `post` or `page`; fetching additionally requires `flavorAgentData.canRecommendContent`                                                                                                                                                              | Hidden for unsupported post types; when unavailable the panel stays visible with a shared setup/capability notice pointing admins to `Settings > Connectors` for text generation; `edit` and `critique` modes require existing draft content                                                                                                                                             | Generate draft text, revision text, or critique guidance. Before usable output, the full composer is visible. After a usable response, the latest generated output becomes primary, the composer collapses behind `Refine request`, notes/issues collapse under `Editorial Notes`, and recent diagnostics collapse under `Recent Content Requests`. Stale results stay readable with refresh, but generated-content copy is disabled until refreshed.                                                                                                                                             | Editorial-only; copy-to-clipboard only; no Flavor Agent apply or undo                                                                                                                                                                                                                       |
| [Navigation recommendations](features/navigation-recommendations.md)       | Nested section inside the block `AI Recommendations` panel for selected `core/navigation` blocks                                                                                                                                                     | The selected block is `core/navigation`; fetch additionally requires `flavorAgentData.canRecommendNavigation` and a menu ID or serialized markup                                                                                                                                    | Hidden for every other block; when unavailable the section stays visible only as a settings or capability notice and hides the composer/results; advisory-only through v1.0                                                                                                                                                                                                              | Preserve a separate navigation request lifecycle and freshness model from block recommendations. The mounted first-party UI is the embedded block flow: a lighter nested shell that preserves stale results with refresh. Background `resolveSignatureOnly` revalidation detects server-side drift in saved menus, overlay context, and theme tokens. `Navigation Ideas` is the embedded wrapper title; unmounted standalone component branches are implementation fallback code, not a current first-party surface | No apply contract or inline undo; scoped read-only `request_diagnostic` audit rows can still appear in the admin activity log when document scope is available                                                                                                                              |
| [Template recommendations](features/template-recommendations.md)           | Site Editor `PluginDocumentSettingPanel` named `AI Template Recommendations`                                                                                                                                                                         | The current edited entity is `wp_template` and the shared entity contract resolves; fetching additionally requires `flavorAgentData.canRecommendTemplates`                                                                                                                           | Hidden outside template editing or before the `wp_template` entity contract resolves; when unavailable after that point, the panel stays visible with the shared setup/capability notice pointing admins to `Settings > Connectors` for text generation; non-deterministic ideas stay advisory-only; hard template entity changes clear recommendations, while same-template prompt or live-context drift stays visible as stale until refresh | Follow the shared scope/freshness -> prompt -> status/stale refresh -> explanation -> grouped `Review first` / `Manual ideas` lanes -> lower review panel -> apply -> undo/history model. Includes clickable entity links and explicit preview-confirm-apply for validated operations, including bounded pattern inserts that must declare explicit placement                                                                                                                                                                                                                                                     | Deterministic apply only after preview; latest valid activity tail can be undone                                                                                                                                                                                                            |
| [Template-part recommendations](features/template-part-recommendations.md) | Site Editor `PluginDocumentSettingPanel` named `AI Template Part Recommendations`                                                                                                                                                                    | The current edited entity is `wp_template_part` and the shared entity contract resolves; fetching additionally requires `flavorAgentData.canRecommendTemplateParts`                                                                                                                   | Hidden outside template-part editing or before the `wp_template_part` entity contract resolves; when unavailable after that point, the panel stays visible with the shared setup/capability notice pointing admins to `Settings > Connectors` for text generation; unsupported or ambiguous suggestions stay advisory-only                                                              | Follow the shared intro -> scope/freshness -> prompt -> status/stale refresh -> featured recommendation -> grouped `Review first` / `Manual ideas` lanes -> lower review panel -> apply -> undo/history model, with focus-block links, pattern browse links, and advisory fallback cards                                                                                                                                                                                                                                                                                                                                                                                        | Deterministic apply only after preview; latest valid activity tail can be undone                                                                                                                                                                                                            |
| [Style and theme intelligence](features/style-and-theme-intelligence.md)   | Site Editor Styles sidebar panel for Global Styles and Style Book; both mount only into the native Styles sidebar slot and return `null` if that slot is unavailable | The active Site Editor Styles sidebar resolves a current `root/globalStyles` entity; Global Styles uses `flavorAgentData.canRecommendGlobalStyles`, and Style Book uses `flavorAgentData.canRecommendStyleBook` with requests enabled only after a valid registered Style Book target block resolves | Hidden outside the Site Editor Styles sidebar; within that sidebar, the panels always mount the shared capability notice component, which renders only when a plugin-settings or capability notice is needed; Style Book can also stay visible with scope notices until a valid registered example block is selected; unsupported or non-`theme.json` ideas stay advisory-only                                                                     | Follow the shared scope/freshness -> prompt -> featured recommendation -> grouped `Review first` / `Manual ideas` lanes -> review -> apply -> undo/history model for validated site-level Global Styles changes, validated Style Book block changes, and Global Styles-only theme style variations; these are native Styles-sidebar surfaces rather than post-editor document panels                                                                                                                                                                                                            | Deterministic apply only after review; latest valid style-surface tail entry can be undone while the live config still matches the recorded post-apply state                                                                                                                                |
| [AI activity and undo](features/activity-and-audit.md)                     | `Recent AI Actions` group inside block, template, and template-part recommendation panels; `Recent AI Style Actions` and `Recent AI Style Book Actions` inside the Global Styles and Style Book panels; `Recent Content Requests` inside the content panel; scoped request diagnostics can hydrate into matching inline lists only when their target matches the panel filters, and always remain available to the admin audit feed | The current editor scope has recorded AI actions or scoped read-only request diagnostics for content, block, template, template-part, Global Styles, or Style Book that match the active inline panel filters | Hidden when no scoped entries exist; older executable entries are blocked while newer still-applied AI actions remain; read-only request diagnostics are review-only or failed audit rows                                                                                                                                                                                                                                                                | Review recent executable actions, inspect matching read-only request diagnostics, see the shared status/undo model, and undo the newest valid tail entry for block, template, template-part, Global Styles, or Style Book surfaces                                                                                                                                                                                                                                                                                                                                                                                                                                         | Undo is inline and editor-scoped for executable apply rows; request diagnostics are audit/review-only                                                                                                                                                                                                                                                            |
| [Admin AI Activity](features/activity-and-audit.md)                        | `Settings > AI Activity`                                                                                                                                                                                                                             | Current user has `manage_options`                                                                                                                                                                                                                                                   | Hidden for non-admin users; read-only today                                                                                                                                                                                                                                                                                                                                              | Browse the recent server-backed activity feed, search and filter it, adjust the DataViews view configuration, inspect custom read-only details for diagnostics/request metadata/undo state/before-after summaries, jump back to affected entities and settings                                                                                                                                                                                                                                                                                                                                                   | Audit only; no apply or row-action undo                                                                                                                                                                                                                                                     |
| [Settings and pattern sync](features/settings-backends-and-sync.md)        | `Settings > Flavor Agent`                                                                                                                                                                                                                            | Current user has `manage_options`                                                                                                                                                                                                                                                   | Hidden for non-admin users; validation errors keep prior saved credentials in place                                                                                                                                                                                                                                                                                                      | Review read-only AI Model readiness from `Settings > Connectors`; configure the Embedding Model, Pattern Storage, Developer Docs diagnostics, Guidelines, and Experimental Features groups; inspect credential-source and runtime diagnostics; review sync state; manually run pattern sync; manage legacy guideline fields and JSON import/export tooling                                                                                                                                                                                                                                                                                                                                               | Settings save and sync only; no undo                                                                                                                                                                                                                                                        |

Content request diagnostics render inline only when the supported post/page content panel is mounted in its configured state; otherwise scoped request diagnostics remain available through the admin audit feed.

## Programmatic Surfaces

| Surface                          | Where it lives                                                                                                            | Gating model                                                                             | What it provides                                                                                                                                                                                                    | Reference                                |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| WordPress Abilities API          | Server-registered abilities under the `flavor-agent` category; hydrated by core on supported WordPress 7.0+ admin screens | Each ability is gated by capability, feature registration, and, where relevant, backend availability | Twenty structured ability contracts for recommendations, content drafting/critique, pattern and synced-pattern inspection, block/theme design helpers, docs grounding, theme tokens, style intelligence, and backend status. The 13 helper/read abilities can register without the AI feature gate; the seven recommendation abilities register only when the WordPress AI plugin feature contracts are present and Flavor Agent is enabled. | `docs/reference/abilities-and-routes.md` |
| Helper abilities and diagnostics | Server-registered helper abilities plus the settings-driven readiness/status contract                                     | Per-ability capability gates, plus Cloudflare and provider/backend gating where relevant | Block introspection and block-type listing, registered and synced pattern inspection, template-part listing, active theme plus presets/styles/tokens, backend readiness, and trusted WordPress docs search          | `docs/features/helper-abilities.md`      |
| Flavor Agent REST API            | Routes under `flavor-agent/v1` consumed by the activity UI and settings admin scripts                                     | Per-route capability callbacks plus route-specific validation and sanitization           | Server-backed activity read/write/undo and manual pattern sync; recommendation requests use the Abilities API instead                                                                                              | `docs/reference/abilities-and-routes.md` |

## Quick Mapping

- Block Inspector -> `flavor-agent/recommend-block`
- Content panel -> `flavor-agent/recommend-content`
- Pattern Inserter -> `flavor-agent/recommend-patterns`
- Navigation subsection in Block Inspector -> `flavor-agent/recommend-navigation`
- Template panel -> `flavor-agent/recommend-template`
- Template-part panel -> `flavor-agent/recommend-template-part`
- Global Styles / Style Book panel -> `flavor-agent/recommend-style`
- Activity (read/write) -> `GET/POST /flavor-agent/v1/activity`
- Activity undo -> `POST /flavor-agent/v1/activity/{id}/undo`
- Pattern sync -> `POST /flavor-agent/v1/sync-patterns`; status polling -> `GET /flavor-agent/v1/sync-patterns`

Recommendation surfaces are available through the WordPress Abilities API. The old `flavor-agent/v1/recommend-*` REST paths were removed before public release.

## Update Rule

When a surface changes, update three places together:

1. this matrix
2. the matching file in `docs/features/`
3. `docs/reference/abilities-and-routes.md` if the contract changed
