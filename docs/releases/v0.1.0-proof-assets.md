# v0.1.0 release proof assets — status and blockers

Status as of 2026-07-28, re-confirmed unchanged since 2026-07-20. This file
records what exists, what does not, and the specific blocker for each missing
asset. It exists so the release gate is auditable: nothing here is a placeholder
for an image that was generated, mocked, or reconstructed. Capture them from a
real site or ship without them.

## Inventory

| Asset | Status | Blocker |
| --- | --- | --- |
| `docs/screenshots/activity-audit.png` | **Present** | — Captured from a WP70 `Settings > AI Activity` row with a pending external style apply open in the approval/audit detail panel. |
| `docs/screenshots/settings-readiness.png` | Missing | Publicly reachable staged site + admin auth. No AI credentials strictly required, but a readiness panel showing everything unconfigured is weak proof; a representative still needs at least one configured backend. |
| `docs/screenshots/inspector-recommendation.png` | Missing | Requires a live AI response through `Settings > Connectors`, i.e. real provider credentials on the staged site. |
| `docs/screenshots/content-recommendation.png` | Missing | Same as above, plus a seeded post with enough content for an editorial pass. |
| `docs/screenshots/template-review.png` | Missing | Same as above, in the Site Editor against a block theme with a `wp_template` worth recommending against. |
| `docs/screenshots/global-styles-review.png` | Missing | Same as above, plus a theme whose `theme.json` exposes the style paths the operation targets. |
| `docs/screenshots/pattern-inserter.png` | Missing | Heaviest prerequisite: a configured pattern retrieval backend (Qdrant + Cloudflare Workers AI embeddings, or private Cloudflare AI Search), a **built pattern index**, and Connectors-owned chat for reranking. |

## Why these cannot be captured from the local stack

The repo already ships capture tooling — `npm run audit:screenshot`
(`scripts/browser-run-screenshot.js`), which drives the Cloudflare Browser
Rendering API and has `settings`, `admin`, `block-editor`, and `site-editor`
presets. It cannot be pointed at the local Docker stack, because Cloudflare
renders the page from **its** network: `http://127.0.0.1:8888` is not reachable
from there. That is the structural blocker, not a missing script.

To use it, all of the following must be true at once:

- `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` are set.
- `BROWSER_RUN_DEFAULT_BASE_URL` points at a **publicly reachable** WordPress
  host running this plugin — a staging site or a tunnelled local stack.
- WordPress admin authentication is supplied via `--cookies-file`,
  `BROWSER_RUN_COOKIES_JSON`, or `--extra-headers-file`; every remaining asset
  is behind `/wp-admin/`.
- For every asset except `settings-readiness`, the staged site has working
  provider credentials in `Settings > Connectors`, because the surfaces render
  a real model response. The pattern asset additionally needs a populated
  pattern index.

## Honest characterisation of the gap

Six of seven stills are missing, and each one depends on a staged site holding
live third-party credentials. That is manual release work with real secrets; it
is not automatable from this repository and it is not something to approximate.

`README.md` currently states the release should not ship without the governance
console proof plus at least one strong native editor still. Only the first of
those two exists today. Either capture at least one editor-surface still before
tagging, or consciously relax that self-imposed gate and record the decision —
but do not treat the gate as met.

## The one path that needs no secrets, and why it was not taken

The Playwright Playground harness (`npm run test:e2e:playground`) boots a real
WordPress with this plugin mounted and drives the native editor with a local
browser, so a still could be captured from it without any staged site, admin
credential, or Cloudflare token. That path was deliberately not used on
2026-07-28.

The harness supplies model output from its MU-plugin rather than from a
configured connector. A resulting Inspector or content still would show real
plugin UI rendering a **fabricated** recommendation. That is precisely the
"generated, mocked, or reconstructed" proof this file exists to prevent, and it
would be the weakest possible evidence for the exact claim the screenshot is
meant to support — that the surfaces work against a live provider.

`activity-audit.png` is not a counter-example. Its pending row is *seeded* — real
governance code holding a real record — while the proposed operation is what an
external agent submitted, not model prose the screenshot is passing off as a
live recommendation.

If a harness-sourced still is wanted anyway, it is a maintainer decision, and it
must be captioned in-repo and in release copy as harness output with stubbed
model responses. It does not satisfy the README gate on its own.
